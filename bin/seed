#!/usr/bin/env bash
# Start local postgres, push schema, seed test data, and run API

set -e

npm run startdb

# Wait for postgres to be ready
echo "Waiting for postgres..."
sleep 2

npm run db:push
npm run db:seed
npm run dev
