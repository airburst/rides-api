meta {
  name: Delete Ride
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/rides/{{rideId}}
  body: none
  auth: inherit
}

vars:pre-request {
  rideId: ride-uuid-here
}

docs {
  # Delete Ride
  
  Soft delete a ride (sets deleted flag to true). Requires LEADER or ADMIN role.
  
  ## Authentication
  Required - Bearer token with LEADER or ADMIN role
  
  ## Path Parameters
  - `id`: Ride UUID
}
