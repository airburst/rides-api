meta {
  name: Cancel Ride
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/rides/{{rideId}}/cancel
  body: none
  auth: inherit
}

vars:pre-request {
  rideId: ride-uuid-here
}

docs {
  # Cancel Ride
  
  Cancel a ride (sets cancelled flag to true). Requires LEADER or ADMIN role.
  
  ## Authentication
  Required - Bearer token with LEADER or ADMIN role
  
  ## Path Parameters
  - `id`: Ride UUID
}
